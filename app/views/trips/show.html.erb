
<div class="banner_container">
	<%= image_tag(@trip.parkProfilePic, :class => "banner_pic") %>
	<div class="banner_overlay">
		<div class="banner_overlay_left">
			<p class="banner_trip_name"><%= @trip.trailName %></p>
			<p class="banner_park_name"><%= @trip.parkName %></p>
		</div>
		<div class="banner_overlay_right">
			<a class="full_album_link">See Full Album</a>
		</div>
	</div>
</div>
<div class="blog_container">
<% content = @trip.content.split("`") %>
<% nex = 0 %>
<% (content.length).times do |count| %>
	<% if nex != count %>
		<% next %>
	<% end %>

	<% if content[count] == "title" %>
		<p class="trip_title"><%=content[count + 1]%></p>
		<% nex += 2 %>
	<% elsif content[count] == "text" %>
		<p class="trip_text"><%=content[count + 1]%></p>
		<% nex += 2 %>
	<% elsif content[count] == "image" %>
		<img src= "<%= content[count + 2] %>" class="<%= content[count+1] %>"/>
		<% nex += 3 %>
	<% end %>
<% end %> 
</div>




<div class="button_container">
	<% if !@trip.previous.nil? %>
		<%= link_to "Previous Trip", trip_path(:id => @trip.previous.id, :num => Integer(params[:num])-1), class: "trip_button" %>
	<% end %>
		<%= link_to "View Full Album", trip_path, class: "trip_button" %>

<% if !@trip.next.nil? %>
		<%= link_to "Next Trip", trip_path(:id => @trip.next.id, :num => Integer(params[:num])+1), class: "trip_button" %>
	<% end %>

<!-- <div class="photobox" >
	<iframe src="http://embedsocial.com/facebook_album/album_photos/547391092081755" width="100%" height="1450" frameborder="0" scrolling="no" marginheight="0"  marginwidth="0"></iframe>
</div> -->


